# 手动验收场景

本插件包含大量确定性逻辑（正则提取、行号计算、去重、命名空间归一化等），这些都可以通过 pytest 离线验证。但以下场景涉及大语言模型（LLM）对语义、上下文和证据优先级的判断，**无法通过确定性单元测试断言**，必须在接入真实 LLM 后手动验证。

## 说明

- 每个场景只描述行为类别，不绑定具体学院、系统或案例。
- 验收时应在真实知识库中构造或找到符合描述的文档，观察 Agent 最终回复。
- 判断标准：回复必须只基于实际读取到的证据 `[E#]`，且符合对应场景的预期约束。

---

## 场景 1：语义适用性判断

**触发条件**：用户问题中的关键词在多份材料中同时出现，但材料回答的是不同主体或不同动作。

**预期行为**：
- 模型不能仅因为关键词命中就把材料当作结论依据。
- 模型应确认证据中的主体、动作、属性、修饰对象、条件和适用范围与用户问题一致。
- 若关系不一致，应继续检索或回复资料不足。

**不可断言原因**：LLM 对“主体-动作关系是否一致”的理解无法离线穷举。

---

## 场景 2：现行规则 vs 历史材料

**触发条件**：用户询问“今年、当前、现在、最新”或未指定年份的现行规则，而知识库中同时存在未标记版本、旧版或归档材料。

**预期行为**：
- 优先引用未标记旧版/归档、状态为 `current` 的材料。
- 若只有 `historical` 或 `archived` 材料，必须先说明“当前信息未找到”，再旧资料作为带年份的历史参考。
- 不能直接把历史年份的日期、流程、要求当作当前事实。

**不可断言原因**：版本元数据可以离线提取，但 LLM 是否真正按优先级引用、是否正确转述“当前未找到”需要语义判断。

---

## 场景 3：近似名称的正式对应

**触发条件**：用户使用的名称与知识库正式名称不完全一致（口语化、缩写、旧称等）。

**预期行为**：
- 模型先用用户原始名称搜索。
- 只有在知识库材料明确证明两个名称指向同一对象时，才能在回答中说明“知识库中的正式名称是××”。
- 不能仅凭文字相似或模型常识自行“纠正”用户。

**不可断言原因**：是否为“明确证明”依赖 LLM 对文本等价关系的理解，无法通过字符串匹配完全覆盖。

---

## 场景 4：残缺/截断证据

**触发条件**：`read_doc` 返回的内容恰好截断在句子中间，或上下文不足以支撑完整结论。

**预期行为**：
- 模型不得补全残句、不得编造后半句。
- 应说明资料不完整，或建议用户读取更大范围/更完整文档。

**不可断言原因**：截断位置和大模型是否“脑补”都是语义层面的判断，离线无法穷举所有截断形式。

---

## 场景 5：QA 文档块独立判断

**触发条件**：一篇 QA 文档中包含多个问答块，其中某些块标记为 `resolved`，另一些块标记为 `no_answer_found`，且它们涉及不同子主题。

**预期行为**：
- 对于有正面证据的子主题，模型应正常回答，不受相邻 `no_answer_found` 块的干扰。
- 对于只有 `no_answer_found` 的子主题，模型应说明暂无可靠资料，不能用其他块的侧面信息推断。

**不可断言原因**：块拆分可以离线验证，但 LLM 是否把 `no_answer` 当作“该文档整体无答案”而误伤其他子主题，需要真实生成验证。

---

## 真实问题手动验收记录

以下问题直接对应南京大学知识库常见查询。验收时接入真实 LLM，逐题记录工具调用链与证据细节，不依赖 pytest 断言。

### 记录字段说明

| 字段 | 填写要求 |
|------|----------|
| 工具调用 | research 阶段实际调用的工具名及参数（如 grep_local_docs、read_doc、get_doc_outline） |
| 查询词 | 进入检索工具的关键词 / query |
| 候选文件 | grep/search 返回的候选 title 与 path |
| read_doc 行号 | 最终进入 Evidence 的 read_doc start_line—end_line |
| Evidence 版本元数据 | `version_status`、`applicable_years`、`applicable_cohorts`、`document_year` |
| available_ids | answer 阶段可使用的 `[E#]` 列表 |
| used_ids | 模型最终在回答中引用的 `[E#]` 列表 |
| 最终结果 | 模型生成的回答原文（保留 `[E#]` 标记） |

### 问题列表

#### 1. 校园卡过期是什么意思？

- **预期关注点**：区分“校园卡过期”这一说法在知识库中的真实定义，避免模型用“补办/挂失”等侧面信息推断。

| 字段 | 记录 |
|------|------|
| 工具调用 | （接入 LLM 后填写） |
| 查询词 | |
| 候选文件 | |
| read_doc 行号 | |
| Evidence 版本元数据 | |
| available_ids | |
| used_ids | |
| 最终结果 | |

#### 2. 全面整理南京大学大一选课注意事项

- **预期关注点**：回答需基于多份文档的正面证据；不能因某份文档标记 `no_answer_found` 而忽略其他有效块。

| 字段 | 记录 |
|------|------|
| 工具调用 | |
| 查询词 | |
| 候选文件 | |
| read_doc 行号 | |
| Evidence 版本元数据 | |
| available_ids | |
| used_ids | |
| 最终结果 | |

#### 3. 今年选课什么时候开始？

- **预期关注点**：优先引用 `current` 或年份最新的材料；若只有历史年份材料，必须先说明“当前信息未找到”。

| 字段 | 记录 |
|------|------|
| 工具调用 | |
| 查询词 | |
| 候选文件 | |
| read_doc 行号 | |
| Evidence 版本元数据 | |
| available_ids | |
| used_ids | |
| 最终结果 | |

#### 4. 开甲学院大一要学什么？

- **预期关注点**：近似名称（如“开甲学院” vs 知识库正式名称）需有明确证据支撑才能对应；不能凭模型常识“纠正”。

| 字段 | 记录 |
|------|------|
| 工具调用 | |
| 查询词 | |
| 候选文件 | |
| read_doc 行号 | |
| Evidence 版本元数据 | |
| available_ids | |
| used_ids | |
| 最终结果 | |

#### 5. 知识库有哪些一级分类？

- **预期关注点**：导航类证据（list_knowledge_bases / list_repo_tree）不得被计为 `read`；回答只能基于工具返回的真实分类列表。

| 字段 | 记录 |
|------|------|
| 工具调用 | |
| 查询词 | |
| 候选文件 | |
| read_doc 行号 | |
| Evidence 版本元数据 | |
| available_ids | |
| used_ids | |
| 最终结果 | |

---

## 验收记录模板

| 场景 | 测试问题（自拟） | 引用证据编号 | 结果 |
|------|------------------|--------------|------|
| 1    |                  |              |      |
| 2    |                  |              |      |
| 3    |                  |              |      |
| 4    |                  |              |      |
| 5    |                  |              |      |

结果填写：`通过` / `不通过`（需附不符合点）。
